# CalCrony — AIDLC State

**Project type**: Greenfield
**Workflow**: Inception executed via Claude Code plan mode (2026-07-17); Construction in progress.

## Inception (complete)
- [x] Workspace detection — greenfield, new repo `CalCrony`
- [x] Requirements analysis — standard depth (see `inception/requirements/requirements.md`)
- [x] Workflow planning — approved plan at Claude plan file; milestones 0–4 defined
- [ ] User stories — skipped (personal project, single stakeholder, requirements captured directly)
- [ ] Application design — captured inside approved plan (architecture, data model, API surface)

## Construction (in progress)
- [x] Milestone 0: Repo bootstrap (private GitHub repo JJWren/CalCrony + local repo)
- [x] Milestone 1: Foundation — solution scaffold, EF Core + Postgres, API-key auth middleware, health endpoint, docker-compose, InitialCreate migration, smoke tests green
- [x] Milestone 2: Events + RSVPs — event/RSVP/settings endpoints, NL datetime parser (Recognizers + NodaTime), bot /create /list /edit /delete /settings /timestamp, RSVP buttons with live embed updates; 15 tests green (incl. Testcontainers Postgres integration)
- [x] Milestone 3: Reminders & notifications — DeliveryScheduler sweep (notifications due, Scheduled→Started ping, Started→Ended), Delivery outbox + pending/ack endpoints, /reminders endpoint, bot DeliveryPollerService + /remind + /notify; 18 tests green
- [x] Milestone 4: ICS feed export — IcsFeedToken entity, anonymous /feeds/{token}.ics (Ical.Net), POST /guilds/{id}/feed-token, bot /link; 20 tests green; full docker-compose E2E verified (health, NL event create, ICS feed)

## Operations
- [x] CI/CD & process (2026-07-17): GitHub Actions CI (build-test + pr-title lint), release-please auto-versioning from conventional commits, GHCR image publishing (calcrony-api / calcrony-bot), squash-only GitHub Flow with CONTRIBUTING.md, issue forms + label taxonomy. First release: v0.1.0.
- [ ] Deployment target TBD — docker compose pulling GHCR images on personal machine/VPS

## Road to v1.0.0 (established 2026-07-19)

Milestone order: M9 series rule editing → M10 Discord native-event mirroring → M11 ICS RRULE emission → M12 event templates → M13 attendee roles → M14 event threads → M15 hardening + full live E2E, released as **v1.0.0** (`Release-As: 1.0.0` in the squash commit footer). Google Calendar push sync is deferred to v2.0.0.

## Construction (continued) — post-v0.1.0 milestones
- [x] Milestone 5: Google Calendar Availability (2026-07-17) — CalendarConnection/CalendarLinkToken entities + AddCalendarAvailability migration; ICalendarProvider abstraction + GoogleCalendarProvider (Google.Apis.Auth, hand-rolled freebusy POST); token encryption via ASP.NET Core Data Protection persisted to a new `dpkeys` volume; anonymous /oauth/google/start+callback (API's first non-JSON surface) with the link-token doubling as OAuth `state`; live (uncached) free/busy via CalendarAvailabilityService with concurrency-safe per-user token refresh; POST /calendar/availability + connection endpoints; bot /calendar connect|status|disconnect (DM-capable) and /availability role|event (on-demand grid, not a /create or RSVP hook); 39 tests green (19 new: FakeCalendarProvider substituted via a new ApiFixture ConfigureTestServices/ConfigureWebHost extension point). Branch `feature/calendar-availability`.

- [x] Milestone 7 PR 1 (2026-07-18): Polls & time polls — Poll/PollOption/PollVote model (+AddPolls migration), nine endpoints with event-parity guards, atomic set-replacement voting, voter-added options, NL close deadlines with scheduler auto-close, computed winners, time-poll→event conversion via the PostEventMessage outbox (poll's channel, 128-char title truncation, ConvertedEventId idempotency); bot /poll create|time|close|convert, button voting ≤5 / select menu 6-10 with automatic boundary swap, first Discord modal (➕ add option), three new poller delivery handlers; delivery types 6/7/8. 110 tests green.
- [x] Milestone 8 PR 1 (2026-07-19): Recurring events (API + bot) — EventSeries/SeriesNotification model (+AddRecurringEvents migration) with a partial unique index enforcing one live occurrence per series; anchor-based RecurrenceCalculator (every-N day/week/month, monthly same-date clamp + nth-weekday with 5th→last, DST-lenient, downtime catch-up); SeriesMaterializer spawning template clones (fresh RSVPs, notifications with lineage) via the PostEventMessage outbox; scheduler sweep self-heals freed slots; skip (→Cancelled + next spawn) / stop (live survives) / delete-live-stops-series; ask-per-edit scope on event PATCHes and notifications; bot /create repeat options, /edit scope, /series skip|stop|info, 🔁 embed line. 147 tests green (31 new).
- Note (2026-07-19): the M11 merge's push event never fanned out to Actions (zero check suites on b08bafd; GitHub reported healthy) — this chore commit exists to re-trigger release-please for v0.11.0.
- [ ] Milestone 15 (in progress 2026-07-20): Hardening + full live E2E → v1.0.0 — hardening audit found guard coverage solid; five PRs land the gaps: (1) input validation (FieldLimits single-source + friendly 400s replacing Postgres truncation 500s across events/notifications/reminders/convert; duration 1-40320 fixing the negative-duration end-before-start defect), (2) 90-day retention sweep (RetentionService/RetentionBackgroundService — Sent/Failed deliveries + login states + refresh tokens + link tokens by creation age; Pending NEVER purged; first purge at boot; clamped config), (3) startup fail-fast (StartupConfigValidation for signing-key rules incl. web-login-without-key; keyless-boot throw; friendly DB-unreachable message; new anonymous /health/ready with DB probe), (4) compose hardening (restart: unless-stopped ×4, api healthcheck on /health/ready + wget in the image, service_healthy dependencies, standalone docker-compose.prod.yml from GHCR images with required-secret guards), (5) go-live docs (README checklist w/ signing key + invite URL permissions=335275969536 + prod compose) + this build-and-test/ scaffolding. Remaining: Joshua's live E2E pass (live-e2e-script.md sections 0-14; results into build-and-test-summary.md), fix PRs for findings, then `docs: prepare v1.0.0` whose squash COMMIT footer carries `Release-As: 1.0.0`.
- [x] Milestone 14 (2026-07-19): Event threads — `Event.WantsThread`/`Event.ThreadId` + `EventSeries.WantsThread` (+AddEventThreads migration); opt-in per event (`/create thread:` + web checkbox — honored for BOTH caller types, unlike attendee-role selection); the bot opens a public thread on the embed message at both post sites (`/create` and the PostEventMessage handler, which also covers series spawns and poll conversions) via best-effort `EventThreadManager` (create/add/archive, never throws), recording the id through BotOnly `PUT /events/{id}/thread`; "Going" RSVPers are auto-added through `AddThreadMember = 12` deliveries (dedup-only coalescing in `EventThreadSync` — add-only, no removal on switch-away); the thread archives via `ArchiveThread = 13` at all four live-exit transitions (sweep end, delete — the embed's deletion does NOT delete its thread, skip, PATCH cancel); spawned occurrences inherit WantsThread and open their own thread; invite permissions 8590019584→335007534080 (adds Create Public Threads / Send Messages in Threads / Manage Threads; later 335275969536 when M15 E2E finding #2 added the Manage Roles bit M13 had missed) with the /create precheck bailing friendly when the channel denies thread creation.
- [x] Milestone 13 (2026-07-19): Attendee roles — `Event.AttendeeRoleId`/`EventSeries.AttendeeRoleId` (+AddAttendeeRoles migration); grant/revoke delivery types 10/11 with a shared self-contained `AttendeeRolePayload` (survives event deletion); pure `AttendeeRoleSync` (min-SortOrder "Going" detection, crossing-on/off decision, rapid-toggle coalescing that nets never-served opposite pairs to zero, fan-out helper); complete enqueue-site inventory: RSVP put/delete (bot AND web callers), sweep Started→Ended, event delete (captured before the RSVP cascade), series skip, PATCH cancel, and role change/clear re-sync (revoke-old + grant-new so old grants can't leak); occurrences inherit the series role at materialization, Series-scope edits update the template only when the request touched the role; bot `AttendeeRoleManager` (best-effort never-throw, idempotency check via socket cache, REST fallback) + pure `AttendeeRoleSpec` prechecks (@everyone/managed/Manage Roles/hierarchy) on /create `attendee-role` and /edit `attendee-role`/`clear-attendee-role`; 🏷️ embed + reply lines; web EventDetail role chip + EventForm clear checkbox (selection stays bot-only — the web has no role list). Role clearing allowed from web, selection 400s.
- [x] Milestone 12 (2026-07-19): Event templates — EventTemplate/EventTemplateNotification (+AddEventTemplates migration, (GuildId,Name) unique index, cap 25, CI-duplicate 409); save-from-existing-event only (content + current notifications + rule when the source is a live series occurrence; fully denormalized); single server-side application path: CreateEventRequest gains TemplateId/NoRecurrence — explicit fields win, template fills gaps, specs always copied onto the event AND onto series specs with lineage when recurring; bot /template save|list|delete + /create template picker (TemplateNameAutocompleteHandler/TemplateFinder, RepeatChoice.None suppression); web Templates tab (GuildTemplates), EventForm start-from-template picker with ?template= preselect, EventDetail save-as-template reveal. Guards: any member saves/uses, creator-or-manager deletes. 216 tests green.
- [x] Milestone 11 (2026-07-19): ICS RRULE emission — feed-only; "history is concrete, the future is projected": each running series emits one RRULE-bearing VEVENT (stable `{seriesId}@calcrony` UID) anchored on its live occurrence, which is skipped in the concrete loop (structural de-dup — past rows predate DTSTART, projection starts at it); gap case (no live occurrence between end/skip and sweep spawn) projects from RecurrenceCalculator.NextOccurrence. Pure `IcsRecurrence.BuildPattern` maps rules exactly: clamped day-of-month via BYMONTHDAY=28..d + BYSETPOS=-1 (plain BYMONTHDAY=31 would skip short months), nth-weekday via positional BYDAY with 5th→-1 (exact — a 5th weekday is always also the last), UNTIL = inclusive local date end-of-day in the series zone → UTC, COUNT = remaining occurrences (anchor-counted vs computed-next modes). ~200 tests green.
- [x] Milestone 10 (2026-07-19): Discord native-event mirroring — opt-in `Guild.MirrorNativeEvents` (bot enable path prechecks Manage Events; web toggle carries a permission hint; flag gates creation only — mirrored events keep syncing regardless), `Event.NativeEventId` + BotOnly `PUT /events/{id}/native-event` write-back, `NativeEventMirror` helper (pure BuildSpec mapping w/ 100/1000/100 caps + RSVP pointer line; best-effort Try* surface that never fails deliveries), hooks across EventModule/SeriesModule/DeliveryPollerService (EventStart promoted to a handler that also flips ACTIVE; new `CompleteNativeEvent = 9` delivery at the previously-silent Started→Ended transition, completing or deleting-if-never-started), delete/skip payloads carry guild+native ids (MessageId widened nullable), invite permissions 84992→8590019584 (adds Manage Events; pinned test updated intentionally), RMW flag preservation fixed at all three settings write sites. 192 tests green.
- [x] Live-testing fixes (2026-07-19, post-M9): zone abbreviations in datetime text ("10:00 AM CST") now override the caller zone via an IANA mapping (Recognizers silently ignored them, so unconfigured-UTC servers parsed wall-clock times as past); event-name params across all bot commands gained a Discord autocomplete picker (title + start time + past marker, id-backed values) plus exact-title-match priority in EventFinder ("Test" vs "TestTitle"); series→one-off conversion is reachable from both edit surfaces ("doesn't repeat" choice on /series edit and the web schedule editor → stop). 177 tests green.
- [x] Milestone 9 (2026-07-19): Series rule editing — PATCH /series/{id} with the "apply and (re)activate" invariant (success always leaves the series running with a computable future occurrence; edits that would leave none 400 toward stop; revive is the same code path, incl. an empty PATCH on a stopped series), SeriesEndChoice discriminator contract (Keep/Never/Until/Count), effective-values validation with count-floor and no-future checks, embed sync for web callers; bot /series edit (includePast finder so fully-ended series stay reachable) with resumed-reply variants; web Edit-schedule card on EventDetail (lazy prefill from GET /series, explicit-values submit, "Save and resume" on ended series + revive card on past-occurrence pages). ~168 tests green.
- [x] Milestone 8 PR 2 (2026-07-19): Recurring events web experience — EventForm "Repeats" section (kind/interval/end-condition controls with live until-date parse preview and a client-side rule summary), edit-mode "Apply to: this occurrence / whole series" ask, EventDetail 🔁 chip + Skip occurrence / Stop repeating confirm-then-act (skip navigates to the replacement), series-aware delete copy, notification add/remove scope asks, EventCard 🔁 badge, GetSeries/StopSeries/SkipOccurrence client methods + scoped notification delete. 152 tests green (5 new bUnit).
- [x] Milestone 7 PR 2 (2026-07-18): Polls on the web — 8 CalCronyWebApiClient poll methods, Events|Polls pill switcher (GuildTabs) on both guild pages, GuildPolls list with open/closed toggle, PollForm (standard/time toggle, dynamic 2–10 option rows with live NL slot previews, closes preview, single-vote hidden for time polls), PollDetail + PollVotePanel (single-vote set/clear + multi-vote XOR toggles as full set-replacement PUTs, own-vote highlighting that survives anonymity, proportional bars, add-option with preview, close/delete confirm-then-act, convert card → navigates to the created event); Docs + README rows. 116 tests green (6 new bUnit).

## Known constraints
- ~~Branch protection/rulesets unavailable (free-plan private repo)~~ Resolved 2026-07-17: repo made **public** (Joshua's call, aware of the one GitGuardian alert on the initial docker-compose dev-placeholder credentials — now parameterized via `CALCRONY_DB_*` env vars, though the literals remain in git history) and the `protect-master` ruleset is active, mirroring FairShare's ruleset adapted to squash-only + required `build-test`/`pr-title` checks, with repository-admin bypass for solo-maintainer merges.
- ~~Google OAuth consent screen must stay in "Testing" status~~ Resolved 2026-07-20: the consent screen is **published (In production)** — refresh tokens no longer expire at 7 days; users see the unverified-app interstitial until Google's sensitive-scope verification completes (submitted; hosted /privacy + /terms pages and homepage branding added for it). `CalendarAvailabilityStatus.ReconnectRequired` remains for legacy testing-era tokens and rare revocations.
- `/availability role` requires the privileged "Server Members Intent" enabled in the Discord Developer Portal, and real end-to-end verification against live Google accounts (§15 of the milestone plan) has not yet been run — deployment prerequisites (Google Cloud OAuth app, public HTTPS reachability) are still outstanding.

## Construction (continued) — 2026-08-31 roadmap (issues #118–#125)
- [x] Quick-ship batch: yearly recurrence (#126) and the auto-updating Live List (#127) — released in v0.26.0.
- [x] RSVP v1 (#120, 2026-08-31): custom options, attendee limits with waitlists, close-early — PR #129 merged after 6 Copilot rounds, released in v0.27.0; review follow-ups in PR #133.
- [x] Public web calendar (#121, 2026-08-31): opt-in per server, unguessable regenerable slug (`/c/{slug}`), login-free noindex month view of concrete + projected events — PR #130 merged after 6 Copilot rounds; the round-6 summary point (manager controls during a guild switch) ships in a follow-up PR.
- [x] Multi-day-of-week recurrence (#122, 2026-09-01): checkbox day sets — PR #135 merged (v0.29.0), driven by the parallel session.
- [x] DM reminders (#123, 2026-09-01): strict per-user opt-in (default off), one-time ephemeral offer after a first seated attending RSVP, outbox-delivered DMs with API-side claims that self-disable on closed DMs — PR #137 merged, released in v0.30.0.
- [x] Action log + CSV export (#124, 2026-09-01): PR #139 merged, released in v0.31.0.
- [x] Docs readability pass (2026-09-02): README Features regrouped into six one-line-bullet groups (~1,430 → 392 words) linking to the web Docs page, which gained a grouped "Features in depth" section as the single long form (one h2#features, six h3 groups, one entry per feature naming its Discord and web routes, permission and default); command rows on both pages trimmed to flags; intro and landing tagline say hosted-or-self-hosted (ADR 0002); sesh gap analysis marks RSVP v2 shipped. Plan: `construction/plans/docs-readability-code-generation-plan.md`, all 15 steps ticked. PR #157 merged after 8 Copilot rounds — seven raising accuracy findings (including pre-existing command-syntax errors: `native-events enabled:true`, `public-calendar mode:`), the eighth an approval with zero comments — released v0.36.0; test on `:main`, prod rolled 0.35.0 → 0.36.0 with backup `calcrony-prod-pre-0.36.0-20260902T182446Z.sql`. Keep both pages in step: `DocsPageTests` pins the structure. Follow-up fix v0.36.1 (PR #160, same day): the page's in-page links had used a fragment-only href, which Blazor resolves against `<base href="/">` and sends to the landing page — links now carry `/docs#features`, and a fresh load of that URL scrolls to the section after the first render (`calcronyPage.scrollToFragment`); both pinned by tests. Prod rolled 0.36.0 → 0.36.1 with backup `calcrony-prod-pre-0.36.1-20260902T192408Z.sql`.
- [x] RSVP v2 (#125) — all three sub-features shipped (2026-09-02), each its own PR, ordered so §3.6 unblocked the others rather than depending on them.
  - [x] §3.6 per-option attendee roles (2026-09-01): the role moves from the event onto `RsvpOption`, so one event hands out Tank/Healer/DPS from a single signup; event-level `AttendeeRoleId` survives only as shorthand for the *attending* option's role, exactly as `AttendeeLimit` is shorthand for that option's `Capacity` (keep that parallel when extending). Edit path collapsed to one per-user diff of `SeatedRoles(ev, live)` before/after. Spec-level roles stripped on create and carried over on edit (`StripSpecRoles`/`CarryOverSpecRoles`) — roles are bot-only as a *security* boundary, since a web caller supplying an arbitrary role id could hand out any role the bot outranks. Migration backfills before dropping, both directions executed against postgres:17-alpine over seeded edge cases. PR #144 merged, released v0.32.0; 632 tests green.
  - [x] §3.5 role-restricted signup (2026-09-02): `AllowedRoleIds` on `RsvpOption` (event-level convenience on the request, mirroring `AttendeeLimit`), a poll-level gate, and bot-written role snapshots (`GuildRoles` with a null-name tombstone for deleted roles, `GuildMemberRoles`, `Guild.RolesSyncedAt` under a 30-minute lease the bot renews every 10 minutes) so the web can answer without the API calling Discord; bot checks live, web checks the snapshot and fails closed (409 "RSVP from Discord" / 403 naming the roles, both carrying `role_restricted`); seats survive role loss; configuration is bot-only (`restrict-to:`, `only:` inside `rsvp-options`), the web strips/carries over and may only clear. ADR 0004. PR #150 merged after 22 Copilot rounds (lease, per-guild locks, newly-watched invalidation, entry-only gating, caller-supplied expected state), released v0.34.0; 703 tests green; prod rolled 0.32.0 → 0.34.0 with a pre-upgrade pg_dump.
  - [x] §3.3 multiple RSVPs per user (2026-09-02): per-event opt-in `AllowMultipleRsvps` (series template field; the web may set it — it hands out nothing), attending semantics unchanged (the one flagged option still drives waitlist, threads, availability, DM reminders, live list), a PUT adds a seat in multi mode (single-choice switch path verbatim), option-scoped `DELETE …/rsvps/{userId}/options/{optionId}` (the bare route clears every seat), roles as a per-member SET difference (`AttendeeRoleSync.RolesHeld`/`Diff` replacing one-seat `Decide`; `SeatedRoles` user → set; lifecycle fan-out de-duplicated by (member, role)), 409 on turning it off with multi-holders, unique index `(EventId, UserId)` → `(EventId, UserId, OptionId)`. Migration Down keeps a member's seated attending row (else the earliest), enqueues the owed role revokes and an embed re-render per affected event; **image rollback is safe only before any member holds two rows** (0.34's PutRsvp would move a row onto a held option and hit the widened index) — otherwise run Down first. No option is exclusive (docs warn). No ADR. PR #154 merged after 8 Copilot rounds (four code fixes, two form-state fixes, doc alignment), released v0.35.0; 752 tests green (507 API / 121 bot / 124 web); test stack refreshed to `:main`, prod rolled 0.34.0 → 0.35.0 with pre-upgrade pg_dump `calcrony-prod-pre-0.35.0-20260902T165244Z.sql`.
- [x] Guild-context guard (#163 → PR #164, v0.36.2, 2026-09-02): `/create` from a user-installed CalCrony in a server the bot isn't in (the prod app is user-installable, and Joshua's test guild is exactly such a server for the prod bot) threw NullReferenceException on `Context.Guild` after the defer and hung on "CalCrony is thinking..." — Discord.Net's `[RequireContext(ContextType.Guild)]` only checks for a guild id, and `RunMode.Async` surfaced the exception in the log alone. Fix: server modules registered `[CommandContextType(Guild)]` + `[IntegrationType(GuildInstall)]` so Discord stops offering them there (the user-scoped `/help`, `/timestamp`, `/calendar` stay reachable from DMs and user installs, explicitly), a `RequireBotInGuild` precondition replying with the invoked application's invite link, an `InteractionExecuted` handler that answers unmet preconditions and post-defer exceptions ephemerally, and `DiscordInvite` moved to Contracts as the single source for web and bot. `ModuleContextTests` pins every module's contexts. PR #164 merged after 2 Copilot rounds (a null-safe log label for failed lookups, then approval with zero comments); released 0.36.2; test on `:main` (8163d02), prod rolled 0.36.1 → 0.36.2 with backup `calcrony-prod-pre-0.36.2-20260902T210203Z.sql`; Discord's global command list confirms the 13 server commands at contexts [Guild] / integration types [GuildInstall]. Developer Portal follow-up (Joshua): Guild Install default settings should carry `bot` + `applications.commands` with permissions 335275969536.
